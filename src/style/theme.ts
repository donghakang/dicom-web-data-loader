import { Theme } from '@emotion/react'

const light = {
  background: '#fafafa',
  color: 'rgba(0,0,0,.87)',
}

const dark = {
  background: '#303030',
  color: '#e8eaed',
}
const theme: Theme = {
  light,
  dark,
}

export default theme
